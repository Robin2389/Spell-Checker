"""
This file is outlining the procedure that we will use to write our Spell Checking Program

Required Steps:

import dictionary
import text, or take as input

Parse the dictionary
  RegExpressions Required, so that we can set up the dictionary to be loaded
    We want to convert our text file into a list format
      We will split at '/n'

Trie load function
  This is already written, and will work with a list of strings type dictionary

Trie check function
"""
